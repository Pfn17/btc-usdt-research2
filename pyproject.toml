[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "btc-usdt-research2"
version = "0.1.0"
description = "Research-first BTCUSDT perpetual market data and validation system"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27,<1",
  "websockets>=12,<16",
  "pydantic>=2.7,<3",
  "pydantic-settings>=2.2,<3",
  "orjson>=3.10,<4",
]

[project.optional-dependencies]
test = ["pytest>=8,<9", "pytest-asyncio>=0.23,<2"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]
