{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python -m pip install -r requirements.txt && python -m pip install . --no-deps"
  },
  "deploy": {
    "startCommand": "PYTHONPATH=src uvicorn btc_research.api:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
